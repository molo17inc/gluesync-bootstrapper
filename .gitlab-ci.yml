###                                                  ###
# Automatically generated file, do not modify manually #
###                                                  ###

image: gradle:8.8-jdk21

stages:
  - check
  - test
  - build
  - deploy
  - notify
  - webhook

variables:
  DOCKER_TLS_CERTDIR: ""
  GIT_DEPTH: "1"
  GIT_STRATEGY: "fetch"
  MOLO17_NEXUS_URL: "https://nexus.hq.molo17.com"

workflow:
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG == null'
      when: always
    - when: never
  name: "$PIPELINE_DESCRIPTION"

check_changelog:
  stage: check
  tags:
    - hq-linux-docker
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy" && $RELEASE_TYPE == "GA"'
  script:
    - |
      CHANGELOG_URL="https://api.backoffice.molo17.com/changelog/core-hub/$MODULE_VERSION"
      echo "Checking changelog availability at: $CHANGELOG_URL"
      HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" "$CHANGELOG_URL")
      if [ "$HTTP_STATUS" != "200" ]; then
        echo "ERROR: Changelog for version $MODULE_VERSION not found (HTTP $HTTP_STATUS). Please add the changelog before releasing."
        exit 1
      fi
      echo "Changelog for version $MODULE_VERSION is available."
  interruptible: true

build_test_all:
  stage: test
  retry: 2
  tags:
    - hq-linux-docker
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG == null'
  script:
    - export GRADLE_USER_HOME=`pwd`/.gradle
    - gradle check
  interruptible: true

build_gluesync:
  stage: build
  retry: 2
  parallel:
    matrix:
      - GRADLE_TASK: ':corehub:gsBuild'
        BUILD_TAG: 'core-hub'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':cassandra:gsBuild'
        BUILD_TAG: 'cassandra'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':postgresql:gsBuild'
        BUILD_TAG: 'postgresql-cdc'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':mysql:gsBuild'
        BUILD_TAG: 'mysql-cdc'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':ibm-iseries:gsBuild'
        BUILD_TAG: 'ibm-iseries'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':couchbase:gsBuild'
        BUILD_TAG: 'couchbase'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':mssql:changetracking:gsBuild'
        BUILD_TAG: 'mssql-ct'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':mssql:cdc:gsBuild'
        BUILD_TAG: 'mssql-cdc'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':vertica:gsBuild'
        BUILD_TAG: 'vertica'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':kafka:gsBuild'
        BUILD_TAG: 'kafka'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':solace:gsBuild'
        BUILD_TAG: 'solace'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':mongodb:gsBuild'
        BUILD_TAG: 'mongodb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':sybase:gsBuild'
        BUILD_TAG: 'sybase'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':dynamodb:gsBuild'
        BUILD_TAG: 'dynamodb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':snowflake:gsBuild'
        BUILD_TAG: 'snowflake'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':oracle:xstream:gsBuild'
        BUILD_TAG: 'oracle-xstream'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':oracle:triggers:gsBuild'
        BUILD_TAG: 'oracle-triggers'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':oracle:triggers-legacy:gsBuild'
        BUILD_TAG: 'oracle-triggers-legacy'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':oracle:cdc:gsBuild'
        BUILD_TAG: 'oracle-logminer'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':aerospike:gsBuild'
        BUILD_TAG: 'aerospike'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':singlestore:gsBuild'
        BUILD_TAG: 'singlestore'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':awss3:gsBuild'
        BUILD_TAG: 'awss3'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':cosmosdb:gsBuild'
        BUILD_TAG: 'cosmosdb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':informix:gsBuild'
        BUILD_TAG: 'informix'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':gcpstorage:gsBuild'
        BUILD_TAG: 'google-cloud-storage'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':ravendb:gsBuild'
        BUILD_TAG: 'ravendb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':google-pubsub:gsBuild'
        BUILD_TAG: 'google-pubsub'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':redis:gsBuild'
        BUILD_TAG: 'redis'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':cockroachdb:gsBuild'
        BUILD_TAG: 'cockroachdb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':mariadb:gsBuild'
        BUILD_TAG: 'mariadb-cdc'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':google-big-query:gsBuild'
        BUILD_TAG: 'google-bigquery'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':scylladb:gsBuild'
        BUILD_TAG: 'scylladb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':yugabytedb:gsBuild'
        BUILD_TAG: 'yugabytedb'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':gridgain:gsBuild'
        BUILD_TAG: 'gridgain'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':db2-luw:triggers:gsBuild'
        BUILD_TAG: 'db2-luw-triggers'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':saphana:gsBuild'
        BUILD_TAG: 'sap-hana'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':azure-data-lake:gsBuild'
        BUILD_TAG: 'azure-data-lake'
        ADDITIONAL_LIBS_PATH: ''
      - GRADLE_TASK: ':redshift:gsBuild'
        BUILD_TAG: 'aws-redshift'
        ADDITIONAL_LIBS_PATH: ''
  tags:
    - hq-linux-docker
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
  script:
    - export GRADLE_USER_HOME=`pwd`/.gradle
    - gradle -Psource=GITLAB_CI -PuiReleaseChannel=$UI_RELEASE_CHANNEL -PuiVersion=$UI_VERSION $GRADLE_TASK
    - mkdir libs
    - mkdir -p additional-libs
    - |
      if [ "$ADDITIONAL_LIBS_PATH" != "" ] && [ -d "$ADDITIONAL_LIBS_PATH" ]; then
       echo "Copying additional libraries from $ADDITIONAL_LIBS_PATH..."
       cp -a $ADDITIONAL_LIBS_PATH/* additional-libs/
      else
       echo "No additional libraries to copy for this module."
      fi
    - find . -type d -path "*/build/libs/$BUILD_TAG" -exec cp -r '{}' libs/ \;
  artifacts:
    paths:
      - libs/*
      - additional-libs/*
    expire_in: 1 day
  interruptible: true

deploy_gluesync_linux:
  stage: deploy
  retry: 2
  tags:
    - hq-linux-docker
  image: docker:27.3.1
  services:
    - name: docker:dind
      variables:
        DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
  script:
    - export GRADLE_USER_HOME=`pwd`/.gradle
    - sh scripts/docker-build-fat-core-hub.sh --module-version=$MODULE_VERSION --release-type=$RELEASE_TYPE --supported-deploy-arch=DEPLOY_AMD64_ARM64
  dependencies:
    - build_gluesync
  interruptible: true

deploy_gluesync_windows:
  stage: deploy
  retry: 2
  parallel:
    matrix:
      - WINDOWS_TAG: "ltsc2019"
        WINDOWS_RUNNER_VERSION: "2019"
      - WINDOWS_TAG: "ltsc2022"
        WINDOWS_RUNNER_VERSION: "2022"
      # - WINDOWS_TAG: "ltsc2025"
      #   WINDOWS_RUNNER_VERSION: "2025"
  tags:
    - "hq-windows-shell-$WINDOWS_RUNNER_VERSION"
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
  script:
    - $env:GRADLE_USER_HOME = "$((Get-Location).Path)\.gradle"
    - ./scripts/docker-build-win-fat-core-hub.ps1 --module-version=$MODULE_VERSION --release-type=$RELEASE_TYPE --windows-tag=$WINDOWS_TAG
  dependencies:
    - build_gluesync
  interruptible: true
  
notify_gluesync_update:
  stage: notify
  retry: 2
  parallel:
    matrix:
      - MODULE_TAG: 'core-hub'
      - MODULE_TAG: 'cassandra'
      - MODULE_TAG: 'postgresql-cdc'
      - MODULE_TAG: 'mysql-cdc'
      - MODULE_TAG: 'ibm-iseries'
      - MODULE_TAG: 'couchbase'
      - MODULE_TAG: 'mssql-ct'
      - MODULE_TAG: 'mssql-cdc'
      - MODULE_TAG: 'vertica'
      - MODULE_TAG: 'kafka'
      - MODULE_TAG: 'solace'
      - MODULE_TAG: 'mongodb'
      - MODULE_TAG: 'sybase'
      - MODULE_TAG: 'dynamodb'
      - MODULE_TAG: 'snowflake'
      - MODULE_TAG: 'oracle-xstream'
      - MODULE_TAG: 'oracle-triggers'
      - MODULE_TAG: 'oracle-triggers-legacy'
      - MODULE_TAG: 'oracle-logminer'
      - MODULE_TAG: 'aerospike'
      - MODULE_TAG: 'singlestore'
      - MODULE_TAG: 'awss3'
      - MODULE_TAG: 'cosmosdb'
      - MODULE_TAG: 'informix'
      - MODULE_TAG: 'google-cloud-storage'
      - MODULE_TAG: 'ravendb'
      - MODULE_TAG: 'google-pubsub'
      - MODULE_TAG: 'redis'
      - MODULE_TAG: 'cockroachdb'
      - MODULE_TAG: 'mariadb-cdc'
      - MODULE_TAG: 'google-bigquery'
      - MODULE_TAG: 'scylladb'
      - MODULE_TAG: 'yugabytedb'
      - MODULE_TAG: 'gridgain'
      - MODULE_TAG: 'db2-luw-triggers'
      - MODULE_TAG: 'sap-hana'
      - MODULE_TAG: 'azure-data-lake'
      - MODULE_TAG: 'aws-redshift'
  tags:
    - hq-linux-docker
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
  script:
    - |
      if ! command -v jq >/dev/null 2>&1; then
        curl -fsSL "https://github.com/jqlang/jq/releases/download/jq-1.7.1/jq-linux-amd64" -o /usr/local/bin/jq
        chmod +x /usr/local/bin/jq
      fi
    - sh $CI_PROJECT_DIR/scripts/update-budibase.sh --tag=$MODULE_TAG  --module-version=$MODULE_VERSION --release-type=$RELEASE_TYPE
  dependencies:
    - deploy_gluesync_linux
    - deploy_gluesync_windows
  interruptible: true

notify_webhook:
  stage: webhook
  retry: 2
  tags:
    - hq-linux-docker
  rules:
    - if: '$PIPELINE_TYPE == "build_and_deploy"'
  script:
    - |
      curl -X POST \
        -H "Content-Type: application/json" \
        -d "{\"release_tag\":\"$MODULE_VERSION\",\"channel\":\"${RELEASE_TYPE,,}\",\"triggered_by\":\"$GITLAB_USER_LOGIN\",\"pipeline_id\":\"$CI_PIPELINE_ID\"}" \
        $N8N_BUILD_NOTIFICATIONS
  dependencies:
    - notify_gluesync_update
  interruptible: true